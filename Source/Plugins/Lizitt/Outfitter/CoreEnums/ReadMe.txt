The enumerations in this directory are commonly customized on a per propject basis.  They are separate from rest of 
the outfitter code so they are easier to exclude from code updates, especially in Unity where the entire
directory can be excluded from import when importing packages.